# Gate 0 v10 - 082612 integrated acceptance and fail-closed release

This preregistration freezes the integrated acceptance and progressive-release
decision for the new rawData surrogate and posterior-assisted qNEHVI framework.
It was sealed against clean `main` commit
`aebcde11798c70153fa9cda6bb59c2fcccbef6b0` before the v10 structural benchmark
or final-wheel validation ran.

The inherited scientific result is deliberately unchanged. Gate 0 v5 rejected
the 1000/2000-design representation and quality/regime candidate; v7 accepted
only coordinate/viewer/offline mechanics; all six v8 calibration artifacts are
uncalibrated and non-transferable; and v9 proved that the explicit qNEHVI
composition falls back through common real evaluation without using a surrogate.
No experimental head may control exploitation in this state.

`acceptance_release_plan.json` freezes:

- the seven required formal comparison arms and their current implementation
  gaps, including mandatory hierarchical-CAE plus GPSAF;
- every inherited blocker and the remaining pre-test threshold seals;
- Phase A experimental-only boundaries, Phase B explicit opt-in/full-real
  fallback, and Phase C no-recommendation/no-default-change status;
- the soft-fallback versus hard-stop matrix; and
- nine explicit conditions for a future formal-benchmark re-entry.

`validate.py` is read-only. It verifies hashes and decisions for v5/v7/v8/v9,
the typed blockers exposed by the installed wheel, the unchanged incomplete
performance runner matrix, formal budgets, phase decisions, and re-entry
contract. It never launches a simulator or accesses a protected test locator.

The only v10 real-simulator work allowed by this plan is the existing
`structural-full` baseline regression: three smoke cells plus six measured
NSGA-III/GPSAF cells, 99 planned attempted evaluations, and no performance
interpretation. The formal performance command is restricted to a no-write
`plan`; its current six cells and 12,000 attempts are not a valid 082612 arm
matrix and must not be started.

After the structural run and final installed-wheel checks, a versioned result
receipt records exact run, package, test, documentation, and scientific-boundary
evidence. Historical v5-v9 files remain immutable.

## Frozen result

`acceptance_release_result_receipt.json` records the final wheel/source hashes,
314 package tests, 76 benchmark-automation tests, the 9/9-cell structural run,
the final-wheel revalidation of the two-record v9 fallback canary, and the blocked
formal plan. `validate_result.py` rechecks those bounded artifacts and returns
`valid-complete-integrated-framework-structural-release-performance-not-accepted`.
The result does not activate exploitation, recommend the opt-in composition, or
permit any of TODOs 082608, 082609, 082611, and 082612 to be archived.
