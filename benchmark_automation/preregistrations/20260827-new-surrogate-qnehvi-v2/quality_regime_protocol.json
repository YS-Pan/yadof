{
  "schema_version": 1,
  "protocol_id": "new-surrogate-quality-regime-v1",
  "core_boundary": {
    "owned_by_yadof": [
      "versioned JSON-safe assessment and policy types",
      "per-design/per-field label and weight validation",
      "design-by-field capped and weighted loss aggregation",
      "shared-token masking/downweighting",
      "field-private gated residual path",
      "uncalibrated applicability/regime head API",
      "semantic identity, checkpoint, and bounded diagnostics"
    ],
    "forbidden_in_core": [
      "Chrono field-name or threshold rules",
      "cost-derived trust labels",
      "untracked executable assessment callbacks",
      "rewriting or smoothing recorded rawData",
      "independent Gaussian observation-noise interpretation"
    ],
    "assessment_priority": [
      "version-matched explicit task assessment in record metadata",
      "versioned task policy over explicit task diagnostics",
      "task-declared shape fallback only when diagnostics are absent",
      "declared missing-assessment behavior"
    ]
  },
  "chrono_task_policy": {
    "policy_id": "chrono-trebuchet-contact-regime",
    "policy_version": 1,
    "diagnostic_path": ["task_diagnostics"],
    "ordered_rules": [
      {
        "regime": "failure",
        "match": "any",
        "conditions": [
          {"path": ["released"], "operator": "falsy", "value": null},
          {"path": ["total_time_endpoint"], "operator": "equals", "value": "simulation_cutoff"}
        ]
      },
      {
        "regime": "chatter",
        "match": "any",
        "conditions": [
          {"path": ["launch_runway_reattach_count"], "operator": "greater-than", "value": 0},
          {"path": ["contacts_at_end"], "operator": "greater-than", "value": 0}
        ]
      }
    ],
    "default_regime": "smooth",
    "design_regime_fields": [
      ["trebuchet_ball_x.npz", "values"],
      ["trebuchet_ball_z.npz", "values"],
      ["trebuchet_ball_vx.npz", "values"],
      ["trebuchet_ball_vz.npz", "values"],
      ["trebuchet_arm_combined_normal_stress.npz", "values"],
      ["trebuchet_hanger_combined_normal_stress.npz", "values"],
      ["trebuchet_peak_strength_utilization.npz", "values"]
    ],
    "scalar_fields_remain_independently_trainable": true,
    "field_weights": {"smooth": 1.0, "chatter": 0.25, "failure": 0.2, "unknown": 1.0},
    "shared_weights": {"smooth": 1.0, "chatter": 0.0, "failure": 0.0, "unknown": 1.0},
    "loss_cap": 4.0,
    "numeric_policy_note": "These training weights are architecture controls in the preregistered ablation, not acceptance thresholds. They may be revised only in a new preregistration version before test access."
  },
  "model_semantics": {
    "base": "smooth_base_i(z_global, optional z_group, z_private_i)",
    "residual": "sigmoid(field_regime_logit_i(x)) * field_private_residual_i(z_private_i)",
    "shared_teacher": "field tokens are multiplied by task assessment shared weights before global/group fusion",
    "clean_target_constraint": "smooth field residual target is zero; gated private residual cannot leak when the gate is zero",
    "random_phase_policy": "the model is not required to reproduce parameter-unstable pointwise chatter phase",
    "applicability": "predictor members emit uncalibrated P(smooth); calibration is deferred to 082609",
    "observation_noise": "zero; regime uncertainty is epistemic/structural",
    "joint_draw_identity": "one predictor member identity is fixed across all candidates and fields in a function draw"
  },
  "required_ablations": [
    "no-gating",
    "robust-weighting-only",
    "shared-latent-isolation",
    "gated-private-residual"
  ],
  "evidence_triggered_followup": {
    "mixture_of_experts_allowed_now": false,
    "trigger": "Only a preregistered held-out ablation showing material clean-target leakage after the MVP may open a later mixture-of-experts gate."
  }
}
