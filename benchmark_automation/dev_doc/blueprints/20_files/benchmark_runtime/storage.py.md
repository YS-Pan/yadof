# File blueprint: benchmark_runtime/storage.py

Own confined path resolution, canonical JSON/new-or-atomic writes, manifests, and
diagnostic provenance digests. Digest helpers never implement resume rejection.
