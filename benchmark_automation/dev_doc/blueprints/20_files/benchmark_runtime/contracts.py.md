# File blueprint: benchmark_runtime/contracts.py

Own `BenchmarkError`, `Paths`, schema/constants, and dependency-neutral contract
values. It contains no filesystem, subprocess, yadof, or orchestration behavior.
