
去掉代码中所有地方的绝对路径，比如project\config.py里的：
```
HTCONDOR_PYTHON_EXE = "C:/ProgramData/miniconda3/envs/yadof/python.exe"
```

这套代码需要运行在各种不同的计算机上，对于任意一个文件，都不能假设它保存在某个确定的位置上。

同时不要新建系统环境变量。可以利用已有的环境变量，比如用户安装ansys 和condor后，系统里应该就已经有相应的路径环境变量，这些可以利用。但是不要在代码里定义新的环境变量。不要让用户用这套代码前需要往系统里添加新的环境变量。
