# Add native benchmark simplification TODO

## Context

The user requested a concrete implementation handoff for simplifying the benchmark tool around yadof's modular algorithm contract. The requested product shape has exactly four top-level directories, two root Python files, and no naming or interface that presents the design as a numbered generation derived from an earlier product.

## Change

- Added a root manual TODO that records the verified architectural problems, exact target tree, contracts, execution flow, result model, deletion scope, implementation order, test matrix, engineering thresholds, and completion criteria.
- Made complete `submit/optimization.py` sources opaque strategy inputs so adding a yadof algorithm requires no benchmark code, documentation, or test change.
- Required self-describing baselines, external study requests, exact materialized preflight, complete run-local drivers, snapshot-only resume, and arbitrary-arm reporting.
- Required responsibility-based current naming with no generation markers, substitute entry points, aliases, dual-read behavior, or product-history narration.

## Documentation boundary

This change only creates the requested handoff and records it. It does not execute the TODO, alter benchmark code, move or delete benchmark files, modify scientific claims, create a run, execute a simulator, or access protected data.

## Validation

The new Markdown files were inspected as UTF-8, their repository paths were checked, the requested top-level shape is explicit, and the TODO contains concrete acceptance criteria. Repository diff checks passed before commit.
