# 2026-08-22 12:16 - Require 0.4.0 After Modularization

## Context

- The coordinated surrogate/optimize modularization and workspace submit-side
  composition tasks will invalidate the pre-refactor workspace standard.
- The user is preparing an isolated development checkout for that work and requires
  the completed change set to become yadof `0.4.0`.

## Change

- Added the same explicit release gate to both coordinated toDos: keep the current
  `0.3.0` version throughout partial stages, then bump the single public package
  version to `0.4.0` after implementation, workspace migration, and documentation
  updates are complete and before final wheel acceptance and archival.
- Added phase checkboxes and completion criteria requiring source, CLI, installed
  package, wheel, and current-version acceptance expectations to agree on `0.4.0`.

## Rationale

- Delaying the bump until the coordinated work is complete prevents intermediate
  compatibility stages from being presented as the finished release while making
  the final incompatible workspace boundary unambiguous.

## Impact

- This is a documentation-only planning change. The current package and migrated
  development environment remain at `0.3.0`; the future coordinated implementation
  owns the actual version update and installed-wheel verification.

## Follow-Up

- Execute both coordinated toDos in the isolated modular workspace, apply the
  `0.4.0` release gate at their final acceptance phase, and archive both toDos only
  after the shared completion criteria pass.
