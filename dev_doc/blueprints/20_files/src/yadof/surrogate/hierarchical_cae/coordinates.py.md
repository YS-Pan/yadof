# File blueprint: src/yadof/surrogate/hierarchical_cae/coordinates.py

Own stored/off-grid coordinate validation, encoding, grid construction, and
interpolation. It does not load workspaces, checkpoints, or models.
