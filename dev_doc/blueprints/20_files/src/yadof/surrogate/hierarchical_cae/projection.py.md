# File blueprint: src/yadof/surrogate/hierarchical_cae/projection.py

Own full rawData, current-cost interval, applicability, and coordinate prediction
over a required compatible state. Predicted rawData is transient and never recorded.
