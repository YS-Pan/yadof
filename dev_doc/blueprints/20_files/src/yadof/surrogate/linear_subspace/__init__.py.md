# Blueprint: `surrogate/linear_subspace/__init__.py`

## Contract

Expose typed low-level PCA/SVD codec and ridge-model utilities for diagnostics and
tests. Keep the workspace component factory in the lightweight parent API and do
not import Torch here.
