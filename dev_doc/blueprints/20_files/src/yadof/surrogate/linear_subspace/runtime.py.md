# Blueprint: `surrogate/linear_subspace/runtime.py`

## Contract

Adapt campaign-hot or durable completed named evidence, fit/publish/recover one
strategy/settings-specific state, and convert parameter-only rawData predictions
through current task cost code. GPSAF output contains deterministic costs and
zero-width per-objective intervals. Predicted rawData remains transient and the
runtime exposes no posterior/readiness method.
