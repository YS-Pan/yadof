将这个项目变成一个package，使用方法改为大部分代码安装到python里。而不是像现在这样，所有固定不动的代码都和每次使用需要修改的东西（仿真文件，workflow等）放在一起。
具体要求有：
1. 需要有一个cli，各种命令和工具通过cli调用。cli作为一个新的模块，添加到project目录下。
2. dev_doc,user_doc在package里需要保留。
3. project里的总体结构不变，project里的大部分内容都移动到 python 的 package lib里。
4. 用户使用时的方法是：
    .1 首先在合适的位置创建一个文件夹
    .2 在命令行里把路径移动到这个文件夹，运行 yadof init的命令。这个命令的结果是，在当前位置生成 job_template 文件夹，config.py 文件，它们从package里复制过来。
    这个job_template里只有“运行job时需要用到的文件”，以及“需要由用户提供/修改的文件”。比如api.py就不放到用户文件夹里的job_template里。需要有
    .3 
